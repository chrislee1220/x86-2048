name: Runner smoke test

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Check runner allocation
        run: |
          echo "runner ok"
          date -u
          uname -a
